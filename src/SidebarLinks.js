import React from "react";
import "./Sidebar.css";

const SidebarLinks = ({ Icon }) => {
  return (
    <div className="sidebarLinks">
      <p>options</p>
    </div>
  );
};

export default SidebarLinks;
